---
const { color, title, subtitle } = Astro.props;
const textColor = `text-${color}`;
---

<section class='snap-center'>
    <article
        class='landing-section bg-black h-screen w-screen text-center relative overflow-hidden'
        data-header-color={color}
    >
        <div class='z-30 relative h-full flex flex-col'>
            <header>
                <h1 class={`${textColor} pt-40 text-[40px] font-medium`}>
                    {title}
                </h1>
                <p class={`${textColor} text-sm`}>
                    {subtitle}
                </p>
            </header>
            <footer
                class='flex flex-grow flex-nowrap justify-end flex-col mb-20'
            >
                <slot name='footer' />
            </footer>
        </div>

        <div class='absolute top-0 bottom-0 w-full'>
            <slot name='background' />
        </div>
    </article>
</section>
